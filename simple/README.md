# Exporting a simple scene using the CE.SDK Processor docker container commandline interface

## Running the sample

1. Make sure you've set up [Docker](https://docs.docker.com/engine/install/) or a compatible container engine on your machine.
2. Run `IMGLY_LICENSE=... ./demo.sh` in your shell
